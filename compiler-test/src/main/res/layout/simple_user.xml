<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="user"
            type="com.glureau.compiler.test.api.dto.SimpleGithubUser" />

        <variable
            name="router"
            type="com.glureau.compiler.test.todo.Router" />

    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:orientation="vertical"
        android:padding="10dp">

        <ImageView
            android:id="@+id/geno_avatar_url"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_marginStart="8dp"
            android:layout_marginTop="8dp"
            android:visibility="visible"
            app:glideLoadUrl="@{user.avatar_url}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@color/colorAccent" />

        <TextView
            android:id="@+id/geno_login"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="8dp"
            android:text="@{user.login}"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@+id/geno_avatar_url"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="My Super Name" />

        <TextView
            android:id="@+id/geno_html_url"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:autoLink="web"
            android:text="@{user.html_url}"
            app:layout_constraintStart_toEndOf="@+id/geno_avatar_url"
            app:layout_constraintTop_toBottomOf="@+id/geno_login"
            tools:text="https://glureau.com" />

    </android.support.constraint.ConstraintLayout>
</layout>